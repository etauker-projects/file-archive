export class Metadata {
    archiveFormat: 'json-archive.v1';       // format used by this libary to determine high level structure
    version: string;                        // application specific version of the data structure
}
